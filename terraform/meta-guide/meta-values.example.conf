; Guide-specific values for easy review before apply.
; Actual runtime values are rendered from Terraform vars into /etc/asterisk/meta-guide/meta-sip-values.conf.

[meta_sip_values]
wa_business_phone_number = 15551234567
domain_name = example.com
sip_tls_port = 5061
rtp_start = 10000
rtp_end = 20000